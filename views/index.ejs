<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gerenciador de Vendas e Estoque</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
    <style>
        .menu-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 2rem;
            justify-items: center;
            margin-top: 3rem;
        }
        .menu-card {
            width: 200px;
            height: 200px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            border-radius: 20px;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }
        .menu-card:hover {
            transform: scale(1.05);
            box-shadow: 0px 4px 15px rgba(0,0,0,0.2);
        }
        .menu-card i {
            font-size: 3.5rem;
            margin-bottom: 1rem;
        }
        .logout-btn {
            position: absolute;
            top: 20px;
            right: 20px;
        }
    </style>
</head>
<body class="bg-light">

    <!-- Botão de logout -->
    <div class="logout-btn">
        <a href="/logout" class="btn btn-outline-secondary">
            <i class="bi bi-box-arrow-right"></i> Sair
        </a>
    </div>

    <div class="container text-center mt-5">
        <h1 class="display-5 mb-4">Bem-vindo ao Gerenciador de Vendas e Estoque!</h1>
        <p class="lead">Escolha uma das opções abaixo:</p>

        <div class="menu-container">
            <a href="/produtos" class="btn btn-primary menu-card">
                <i class="bi bi-box-seam"></i>
                <span>Produtos</span>
            </a>
            <a href="/clientes" class="btn btn-success menu-card">
                <i class="bi bi-people"></i>
                <span>Clientes</span>
            </a>
            <a href="/vendas" class="btn btn-danger menu-card">
                <i class="bi bi-cash-stack"></i>
                <span>Vendas</span>
            </a>
            <a href="/carga-produtos" class="btn btn-warning menu-card">
                <i class="bi bi-upload"></i>
                <span>Carga de Produtos</span>
            </a>

            <% if (usuario && usuario.role === 'admin') { %>
            <a href="/usuarios/novo" class="btn btn-info menu-card">
                <i class="bi bi-person-plus"></i>
                <span>Criar Usuário</span>
            </a>
            <% } %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
